import { CountryMeta } from "../types";
import { si } from "./si";
import { de } from "./de";
import { at } from "./at";
import { ch } from "./ch";

export const COUNTRY_META: Record<string, CountryMeta> = {
  si,
  de,
  at,
  ch,
};
